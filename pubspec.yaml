name: fase_2_consumo_api
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  http: ^1.6.0
  dartz: ^0.10.1
  equatable: ^2.0.7
  get_it: ^9.1.1
  meta: ^1.17.0

dev_dependencies:
  build_runner: ^2.10.4
  json_serializable: ^6.11.3
  lints: ^6.0.0
  test: ^1.25.6
